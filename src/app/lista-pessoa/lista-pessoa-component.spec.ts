import { ListaPessoaComponent } from './lista-pessoa-component';

describe('ListaPessoaComponent', () => {
  it('should create an instance', () => {
    expect(new ListaPessoaComponent()).toBeTruthy();
  });
});
